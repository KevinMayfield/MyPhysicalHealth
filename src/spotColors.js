// Shared marker colours for the map and elevation profile, so a given
// kind of flagged spot always reads the same way wherever it's drawn.
const SPOT_COLORS = {
  lowValue: '#6f6c5f',
  junction: '#8a5a2f',
  bonk: '#7a2e3f',
  decouple: '#c9932e',
};

module.exports = { SPOT_COLORS };
